# Data Viewer

This is a standalone project folder created alongside `ollama_project`.

## Isolation Guarantee
- This folder is independent from `ollama_project`.
- No files inside `ollama_project` were modified.

## Next Steps
- Choose a tech stack (Python, FastAPI, Streamlit, React, etc.)
- Initialize dependencies and project structure
